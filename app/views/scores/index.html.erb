<p id="notice"><%= notice %></p>

<h1>Scores</h1>

<table>
  <thead>
    <tr>
      <th>Home</th>
      <th>Guests</th>
      <th>Homescore</th>
      <th>Guestscore</th>
      <th>Overtime</th>
      <th>Winner team</th>
      <th>Winner Points</th>
      <th>Loser</th>
      <th>Loser Points</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scores.each do |score| %>
      <tr>
        <td><%= score.home_team.full_name %></td>
        <td><%= score.guest_team.full_name %></td>
        <td><%= score.home_score %></td>
        <td><%= score.guest_score %></td>
        <td><%= score.overtime %></td>
        <td><%= score.winner_team&.full_name %></td>
        <td><%= score.points_for_winning_team %></td>
        <td><%= score.loser_team&.full_name %></td>
        <td><%= score.points_for_loser_team %></td>
        <td><%= link_to 'Show', score %></td>
        <td><%= link_to 'Edit', edit_score_path(score) %></td>
        <td><%= link_to 'Destroy', score, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Score', new_score_path %>
