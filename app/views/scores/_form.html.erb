<%= form_with(model: score, local: true) do |form| %>
  <% if score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :home %>
    <%= form.collection_select(:home_team_id, Team.all, :id, :full_name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :guests %>
    <%= form.collection_select(:guest_team_id, Team.all, :id, :full_name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :home_score %>
    <%= form.number_field :home_score %>
  </div>

  <div class="field">
    <%= form.label :guest_score %>
    <%= form.number_field :guest_score %>
  </div>

  <div class="field">
    <%= form.label :overtime %>
    <%= form.check_box :overtime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
